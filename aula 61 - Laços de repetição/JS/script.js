window.onload = function(){
    var contador = 0

    /*      WHILE - PRECISAMOS DE UMA VARIAVEL INICIAL 
    while(contador <= 10){
        console.log(` Número: ${contador}`)
        contador++
    }
    */
   /*Mesma coisa que o While, mas de baixo para cima:*/
   do {
    console.log(` Número: ${contador}`)
    contador++
   } while (contador <= 10);

}